import React from "react";

const Title = (props) => {
	return (
		<>
			<div className="text-[128px] font-Cabinet text-white font-bold">
				{props.text}
			</div>
		</>
	);
};

export default Title;
